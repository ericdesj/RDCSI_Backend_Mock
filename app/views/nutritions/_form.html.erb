<%= form_with(model: nutrition, local: true) do |form| %>
  <% if nutrition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nutrition.errors.count, "error") %> prohibited this nutrition from being saved:</h2>

      <ul>
      <% nutrition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :meal_name %>
    <%= form.text_field :meal_name, id: :nutrition_meal_name %>
  </div>

  <div class="field">
    <%= form.label :quality %>
    <%= form.number_field :quality, id: :nutrition_quality %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :nutrition_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
